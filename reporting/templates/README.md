# Report templates

The first reporting version renders directly from Python/ReportLab, so no external document template is required.

This directory is reserved for future reusable PDF/HTML assets such as cover-page text, logos, or branded fragments.
